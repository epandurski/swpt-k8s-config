# TODO: Add to the main repo.
apiVersion: batch/v1
kind: Job
metadata:
  name: splitting-phase-1-job
  labels:
    app.kubernetes.io/component: shard-splitting-job
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/component: shard-splitting-job
    spec:
      podReplacementPolicy: Failed
      priorityClassName: shard-background-worker
      imagePullSecrets:
        - name: regcreds
      containers:
      - name: done  
        image: swpt-split-shard
        args: ['prepare-for-draining']
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        envFrom:
          - configMapRef:
              name: git-ops-config
        env:
          - name: WORKING_DIRECTORY
            value: "/mnt/git-ops"
        resources:
          requests:
            cpu: 10m
            memory: 100Mi
            ephemeral-storage: 1Gi
        volumeMounts:
          - mountPath: "/mnt/git-ops/"
            name: git-repo-working-copy
            readOnly: false
          - name: git-pull-ssh-id
            mountPath: "/etc/ssh/id_rsa"
            subPath: id_rsa
          - name: git-pull-ssh-id
            mountPath: "/etc/ssh/id_rsa.pub"
            subPath: id_rsa.pub
          - name: git-pull-ssh-id
            mountPath: "/etc/ssh/id_rsa-cert.pub"
            subPath: id_rsa-cert.pub
      initContainers:
      - name: verify-shard-content
        image: swpt-accounts
        args: ['verify_shard_content']
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        envFrom:
          - secretRef:
              name: db-owner-credentials
            prefix: pg_
          - secretRef:
              name: broker-default-user
            prefix: rmq_
          - configMapRef:
              name: shardconfig
        env:
          - name: POSTGRES_URL
            value: "postgresql+psycopg://$(pg_username):$(pg_password)@$(pg_cluster_name):5432/db"
          - name: PROTOCOL_BROKER_URL
            value: "$(rmq_connection_string)"
          - name: PROTOCOL_BROKER_QUEUE
            value: "$(pg_cluster_name)"
        resources:
          requests:
            cpu: 10m
            memory: 100Mi
      restartPolicy: OnFailure
      volumes:
        - name: git-repo-working-copy
          emptyDir: {}
        - name: git-pull-ssh-id
          secret:
            secretName: git-pull-ssh-id
            defaultMode: 0600
  backoffLimit: 2147483647
